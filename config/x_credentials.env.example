# 仅在本机保存。复制为 x_credentials.env 后再填写真实内容。
# 不要把此文件上传到 GitHub、网盘或聊天记录。

# X Console -> OAuth 2.0 Keys -> Access Token
X_USER_ACCESS_TOKEN=PASTE_YOUR_ACCESS_TOKEN_HERE

# 可选：留空时，采集器会通过 /2/users/me 自动识别你的 X 账号。
X_USER_ID=

# 可选：仅用于本地测试或代理，不需要改动。
# X_API_BASE_URL=https://api.x.com/2
